topology:
  type: custom
  path: "configs/custom_topology.json"

flows:
  count: 6
  seed: 2024

algorithms:
  heuristics:
    - greedy
    - local_search
    - grasp
    - ga
  ilp:
    enabled: true
    timeout: 60

evaluation:
  repetitions: 1
  load_factors: [0.3]
  k_paths: 3
